{
	"logRoot": "$(PYTHON_LOG_ROOT)",
	"GAMES": {
		"downloadRoot": "$(GAMES_DOWNLOAD_ROOT)",
		"chessbaseDir": "$(GAMES_CHESSBASE_DIR)"
	},
	"TWIC": {
		"downloadDir": "$(TWIC_DOWNLOAD_DIR)",
		"backupDir": "$(TWIC_BACKUP_DIR)",
		"outputDir": "$(TWIC_OUTPUT_DIR)"
	}
}
