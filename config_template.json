{
	"logRoot": "$(PYTHON_LOG_ROOT)",
	"environment": {
		"rabbitmqHost": "$(RABBITMQ_HOST)",
		"rabbitmqUser": "$(RABBITMQ_USER)",
		"rabbitmqPass": "$(RABBITMQ_PASS)",
		"connectionString": "$(CONNECTION_STRING)"
	},

	"ANALYZE": {
		"rabbitmqRoute": "$(ANALYSIS_RABBITMQ_ROUTE)",
		"analysisEnv": "$(ANALYZE_PYTHON_PATH)",
		"analysisProgram": "$(ANALYZE_PROGRAM_PATH)"
	},
	"GAMES": {
		"downloadRoot": "$(GAMES_DOWNLOAD_ROOT)",
		"analysisDir": "$(ANALYSIS_QUEUE_DIR)",
		"chessbaseDir": "$(GAMES_CHESSBASE_DIR)"
	},
	"QUEUEANALYSIS": {
		"rabbitmqRoute": "$(ANALYSIS_RABBITMQ_ROUTE)",
		"analysisDir": "$(ANALYSIS_QUEUE_DIR)"
	},
	"TWIC": {
		"downloadDir": "$(TWIC_DOWNLOAD_DIR)",
		"backupDir": "$(TWIC_BACKUP_DIR)",
		"outputDir": "$(TWIC_OUTPUT_DIR)"
	}
}
